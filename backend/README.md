# Backend API - Operis

## 📁 Структура проекта

```
backend/
├── main.py                    # Главный файл приложения
├── requirements.txt           # Зависимости Python
└── routers/                   # Папка с роутерами (API endpoints)
    ├── __init__.py           # Инициализация пакета
    └── generate.py           # Роутер для генерации ответов
```

---

## 🚀 Быстрый старт

### Установка зависимостей

```bash
cd backend
pip install -r requirements.txt
```

### Запуск сервера

```bash
# Вариант 1: Через Python
python main.py

# Вариант 2: Через uvicorn напрямую
uvicorn main:app --reload --host 0.0.0.0 --port 8000
```

Сервер будет доступен на `http://localhost:8000`

---

## 📋 Описание компонентов

### 1. `main.py` - Главный файл приложения

**Что делает:**
- Создает FastAPI приложение
- Настраивает CORS для работы с фронтендом
- Подключает все роутеры
- Добавляет базовые эндпоинты

**Детали:**

```python
app = FastAPI(title="Operis API", version="1.0.0")
```
- Создает FastAPI приложение с названием и версией

```python
app.add_middleware(CORSMiddleware, ...)
```
- Настраивает CORS (Cross-Origin Resource Sharing)
- Разрешает запросы с любого домена (`allow_origins=["*"]`)
- ⚠️ **Важно:** В продакшене нужно указать конкретные домены вместо `"*"`

```python
app.include_router(generate.router)
```
- Подключает роутер генерации
- Эндпоинт будет доступен по `/generate`

**Базовые эндпоинты:**
- `GET /` - Проверка, что API работает
  - Ответ: `{"message": "Operis API is running"}`
- `GET /health` - Проверка здоровья сервиса
  - Ответ: `{"status": "ok"}`

**Запуск:**
```python
uvicorn.run(app, host="0.0.0.0", port=8000)
```
- Запускает сервер на всех интерфейсах (`0.0.0.0`)
- Порт: `8000`

---

### 2. `routers/generate.py` - Роутер генерации

**Что делает:**
- Обрабатывает запросы на генерацию ответов на отзывы
- Валидирует входные данные через Pydantic
- Возвращает сгенерированный ответ

**Детали:**

```python
router = APIRouter(prefix="/generate", tags=["generate"])
```
- Создает роутер с префиксом `/generate`
- Теги используются для группировки в документации API

**Модели данных:**

```python
class GenerateRequest(BaseModel):
    text: str
```
- Входная модель: текст отзыва клиента
- Обязательное поле: `text` (строка)

```python
class GenerateResponse(BaseModel):
    response: str
    text: Optional[str] = None
```
- Выходная модель: сгенерированный ответ
- Обязательное поле: `response` (строка)
- Опциональное поле: `text` (для обратной совместимости)

**Эндпоинт:**

```python
@router.post("", response_model=GenerateResponse)
async def generate(request: GenerateRequest):
```

- **Метод:** `POST`
- **URL:** `/generate` (полный путь, т.к. префикс уже в роутере)
- **Входные данные:**
  ```json
  {
    "text": "Отзыв клиента..."
  }
  ```
- **Выходные данные:**
  ```json
  {
    "response": "Благодарим за обратную связь...",
    "text": null
  }
  ```

**Текущая реализация:**
- Пока возвращает демо-ответ (заглушка)
- В будущем здесь будет реальная логика генерации через LLM

---

### 3. `requirements.txt` - Зависимости

```
fastapi==0.104.1              # Веб-фреймворк для API
uvicorn[standard]==0.24.0     # ASGI сервер для запуска
pydantic==2.5.0               # Валидация данных
```

**Описание:**
- **FastAPI** - современный веб-фреймворк для создания API
- **Uvicorn** - ASGI сервер для запуска FastAPI приложений
- **Pydantic** - библиотека для валидации данных через типы Python

---

## 🔄 Как это работает вместе

### 1. Запуск сервера

```bash
python backend/main.py
```

Сервер запускается на `http://localhost:8000`

### 2. Запрос от фронтенда

```javascript
fetch('/generate', {
  method: 'POST',
  headers: {
    'Content-Type': 'application/json',
  },
  body: JSON.stringify({ 
    text: "Отзыв клиента..." 
  })
})
```

### 3. Обработка запроса

1. FastAPI получает POST запрос на `/generate`
2. CORS middleware проверяет разрешения
3. Роутер `/generate` обрабатывает запрос
4. Pydantic валидирует данные (`GenerateRequest`)
5. Функция `generate()` обрабатывает запрос
6. Возвращается ответ (`GenerateResponse`)

### 4. Ответ клиенту

```json
{
  "response": "Благодарим за обратную связь. Мы ценим ваше мнение и обязательно учтем ваши замечания для улучшения качества нашего сервиса. Если у вас есть дополнительные вопросы, мы готовы помочь.",
  "text": null
}
```

---

## 📚 API Документация

После запуска сервера доступна автоматическая документация:

- **Swagger UI:** `http://localhost:8000/docs`
- **ReDoc:** `http://localhost:8000/redoc`

---

## 🔧 Что можно улучшить

### 1. Добавить реальную логику генерации
```python
# В routers/generate.py
async def generate(request: GenerateRequest):
    # Интеграция с LLM API (OpenAI, Anthropic, etc.)
    response = await call_llm_api(request.text)
    return GenerateResponse(response=response)
```

### 2. Добавить обработку ошибок
```python
from fastapi import HTTPException

@router.post("", response_model=GenerateResponse)
async def generate(request: GenerateRequest):
    try:
        # Логика генерации
        pass
    except Exception as e:
        raise HTTPException(status_code=500, detail=str(e))
```

### 3. Добавить логирование
```python
import logging

logger = logging.getLogger(__name__)

@router.post("", response_model=GenerateResponse)
async def generate(request: GenerateRequest):
    logger.info(f"Generating response for text: {request.text[:50]}...")
    # Логика генерации
```

### 4. Добавить аутентификацию
```python
from fastapi import Depends, HTTPException, status
from fastapi.security import HTTPBearer

security = HTTPBearer()

@router.post("", response_model=GenerateResponse)
async def generate(
    request: GenerateRequest,
    token: str = Depends(security)
):
    # Проверка токена
    # Логика генерации
```

### 5. Ограничить CORS в продакшене
```python
app.add_middleware(
    CORSMiddleware,
    allow_origins=[
        "https://operis.com",
        "https://www.operis.com"
    ],
    # ...
)
```

### 6. Добавить rate limiting
```python
from slowapi import Limiter
from slowapi.util import get_remote_address

limiter = Limiter(key_func=get_remote_address)

@router.post("", response_model=GenerateResponse)
@limiter.limit("10/minute")
async def generate(request: GenerateRequest):
    # Логика генерации
```

### 7. Добавить кэширование
```python
from functools import lru_cache

@lru_cache(maxsize=100)
def generate_cached(text: str):
    # Логика генерации
```

---

## 📊 Текущее состояние

✅ **Готово:**
- Базовая структура FastAPI приложения
- Роутер для генерации ответов
- Валидация входных/выходных данных
- CORS настроен
- API работает и принимает запросы
- Возвращает демо-ответ

⏳ **В разработке:**
- Реальная логика генерации ответов
- Интеграция с LLM API

---

## 🔗 Связь с фронтендом

Фронтенд отправляет запросы на `/generate`:

```typescript
// src/pages/ReputationControlPage.tsx
const response = await fetch('/generate', {
  method: 'POST',
  headers: {
    'Content-Type': 'application/json',
  },
  body: JSON.stringify({ text: review }),
})
```

**Важно:** В продакшене нужно настроить прокси или указать полный URL бэкенда.

---

## 🐛 Отладка

### Проверка работы API

```bash
# Проверка базового эндпоинта
curl http://localhost:8000/

# Проверка health check
curl http://localhost:8000/health

# Тест генерации
curl -X POST http://localhost:8000/generate \
  -H "Content-Type: application/json" \
  -d '{"text": "Тестовый отзыв"}'
```

### Логи

Логи FastAPI автоматически выводятся в консоль при запуске через `uvicorn`.

---

## 📝 Лицензия

Проект Operis
